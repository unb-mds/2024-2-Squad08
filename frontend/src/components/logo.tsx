import "../styles/logo.css";

const Logo = () => {
  return (
    <div className="logo-background">
      <button className="logo-button">
        MONITORA BSB
      </button>
    </div>
  );
};

export default Logo;
